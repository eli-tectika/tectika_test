{
  "name": "pacman-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "ci": "npm ci && npm run build && npm test"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.2.0",
    "vitest": "^1.6.0",
    "@vitest/browser": "^1.6.0",
    "jsdom": "^24.0.0"
  }
}
